'use strict';

// Replace gOPD.js with native Object.getOwnPropertyDescriptor
var $gOPD = Object.getOwnPropertyDescriptor;

// Check if gOPD is available and works correctly
if ($gOPD) {
  try {
    // Test gOPD on a simple object
    $gOPD([], 'length');
  } catch (e) {
    // If it fails (e.g., in IE 8), set gOPD to null
    $gOPD = null;
  }
}

// Export the gOPD function (or null if it's broken)
module.exports = $gOPD;
'use strict';

// Import the gOPD function from gOPD.js
var $gOPD = require('./gOPD.js'); // Ensure the correct file name and extension

// Check if gOPD is available and works correctly
if ($gOPD) {
	try {
		// Test gOPD on a simple object
		$gOPD([], 'length');
	} catch (e) {
		// If it fails (e.g., in IE 8), set gOPD to null
		$gOPD = null;
	}
}

// Export the gOPD function (or null if it's broken)
module.exports = $gOPD;